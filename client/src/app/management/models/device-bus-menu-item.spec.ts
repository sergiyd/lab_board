import { DeviceBusMenuItem } from "./device-bus-menu-item";

describe("DeviceBusMenuItem", () => {
  it("should create an instance", () => {
    expect(new DeviceBusMenuItem()).toBeTruthy();
  });
});
