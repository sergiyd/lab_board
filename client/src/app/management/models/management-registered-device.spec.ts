import { ManagementRegisteredDevice } from './management-registered-device';

describe('ManagementRegisteredDevice', () => {
	it('should create an instance', () => {
		expect(new ManagementRegisteredDevice()).toBeTruthy();
	});
});
